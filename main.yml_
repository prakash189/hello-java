---
- hosts: all

  become: yes
  become_method: sudo
  remote_user: ubuntu

  tasks:
    - name: Copy to s3
      s3:
          bucket: "publish-artifacts-java"
          object: "/LoginWebApp-1.war"
          dest: "/target/LoginWebApp-1.war"
          mode: put
          overwrite: no
